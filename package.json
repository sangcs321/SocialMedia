{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "dev:client": "cd PublicModule.Client && yarn dev",
    "dev:server": "cd PublicModule.Server && dotnet run",
    "dev": "concurrently \"cd PublicModule.Client && yarn dev\" \"cd PublicModule.Server && dotnet run\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
